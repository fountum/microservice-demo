apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: service-db
  name: service-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
